<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @block.name %>
</p>
<p>
  <strong>Zu erreichende Kredits:</strong>
  <%= @block.credits_min %>
</p>
<p>
  <strong>Veranstaltungen:</strong>
  <%= @block.events.map { |a| a.title }.join(", ") %>
</p>

<%= link_to 'Edit', edit_block_path(@block) %> |
<%= link_to 'Back', blocks_path %>
