<h1>Blöcke</h1>

<!-- changed layout of table -->
<table class="table table-striped">
  <thead>
    <tr>
      <th>Name</th>
      <th>Zu erreichende Credits</th>
      <th>Veranstaltungen</th>
      <th colspan="3"></th>
    </tr>
  </thead>

<!-- changed links to buttons -->
  <tbody>
    <% @blocks.order("name").each do |block| %>
      <tr>
        <td><%= block.name %></td>
        <td><%= block.credits_min %></td>
        <td><%= block.events.map { |a| a.title }.join("<br>").html_safe %></td>
        <div class="btn-group btn-group-justified">
          <td class="col-sm-1"><%= link_to 'Anzeigen', block , class: 'btn btn-default'%></td>
          <% if can? :edit, @blocks %>
          <td class="col-sm-1"><%= link_to 'Bearbeiten', edit_block_path(block) , class: 'btn btn-default'%></td>
          <% end%>
          <% if can? :destroy, @event %>
          <td class="col-sm-2"><%= link_to 'Löschen', block, method: :delete, data: { confirm: 'Sicher?' } , class: 'btn btn-default'%></td>
          <% end%>
          </div>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Block', new_block_path %>
