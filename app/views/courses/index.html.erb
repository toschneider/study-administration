   <%= stylesheet_link_tag "courseOverview.css" %>

<!--- neu -->
<p id="notice"><%= notice %></p>


<ol class = "tree">
  <li>
    <!-- Ebene 1: Courses -->
    <label for="Studiengaenge">Studiengaenge</label>
    <input type="checkbox" id="Studiengaenge" />
      <ol>
      <% @courses.each do |course| %>
        <li>  
          <label for="course"><%=    link_to course.name,  course %></label>
          <input type="checkbox"  id="Bloecke" />
        
            <ol>
              <!-- Ebene 2: Blocks -->
              <li class="file"><%= course.blocks.map { |a| a.name 

                 link_to a.name, a 

                }.join("<br>").html_safe %></li>
            </ol>
        </li>
      
      <% end %>
    </ol>
  </li>
</ol>

<%= link_to 'New Course', new_course_path %>